<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WebNote</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" href="/img/webnotelogo.png">
</head>
<body>
  <header>
    <nav>
      <div class="container">
        <a href="/" class="logo">
          <img src="/img/webnotelogo.png" alt="WebNote logo" width="15%">
        </a>
        <h1>Note</h1>
        <ul class="nav-links"> <!-- these 3 links always show up -->
          <li><a href="/">Home</a></li>
          <li><a href="/users/about">About</a></li>
          <li><a href="/api/inspire">Inspiration</a></li>

          <% if (user) { %> <!-- these only show if user is logged in -->
            <li><a href="/notes">Notes</a></li>
            <li><a href="/notes/list">My Notes</a></li>
            <li><a href="/users/profile">Profile</a></li>
            <li><a href="/users/logout">Logout</a></li>

          <% } else { %> <!-- these only show if user is NOT logged in -->
            <li><a href="/users/login">Login</a></li>
            <li><a href="/users/register">Register</a></li>
          <% } %>
        </ul>
      </div>
    </nav>
  </header>

  <main class="notes-page">
    <div class="note-editor-container">
      <div class="note-editor-header">
        <h1><%= note ? "Edit Note" : "New Note" %></h1>
        <a href="/" class="btn-secondary">Back</a>
      </div>

      <form class="note-editor-form" action="<%= note ? '/notes/' + note.id + '/edit' : '/notes' %>" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <div class="form-group">          <!-- note title area-->
          <label for="title">Title</label>
          <input type="text" id="title" name="title" value="<%= note ? note.title : '' %>" placeholder="Enter note title..." required>
        </div>

        <div class="form-group">         <!-- note content area-->
          <label for="content">Content</label>
          <textarea id="content" name="content" required><%= note ? note.content : '' %></textarea>
        </div>

        <div class="note-editor-actions">
          <button type="submit" class="btn">Save Note</button>
        </div>
      </form>
    </div>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 CS346 Project. All rights reserved.</p>
    </div>
  </footer>

  <script src="/js/main.js"></script>

</body>
</html>
